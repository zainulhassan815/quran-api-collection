meta {
  name: Contents API
  seq: 3
}

headers {
  x-client-id: {{client_id}}
  x-auth-token: {{token}}
}

auth {
  mode: inherit
}

script:pre-request {
  const token = bru.getEnvVar("token");
  if (!token) {
    await bru.runRequest("Get Access Token");
  }
}
